/**
 * Creating a sidebar enables you to:
 * - Create an ordered group of docs
 * - Render a sidebar in the docs side navigation (can be rendered left or right)
 * - Perform other advanced functionalities
 */

/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
  tutorialSidebar: [
    {
      type: 'category',
      label: 'Introduction',
      items: ['introduction/chapter1'],
    },
  ],
};

export default sidebars;
